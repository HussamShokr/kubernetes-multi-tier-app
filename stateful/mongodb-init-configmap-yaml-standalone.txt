apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-init
  namespace: multi-tier
data:
  init.js: |
    db.getSiblingDB("admin").auth("root", process.env.MONGO_INITDB_ROOT_PASSWORD);
    db = db.getSiblingDB("app-database");
    db.createUser({
      user: "app-user",
      pwd: "AppP@ssw0rd!",
      roles: [
        { role: "readWrite", db: "app-database" }
      ]
    });
    db.createCollection("users");
    db.users.insertMany([
      { name: "Test User 1", email: "user1@example.com" },
      { name: "Test User 2", email: "user2@example.com" }
    ]);